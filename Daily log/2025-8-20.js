1. Vite の目的

Vite の主な目的は 「高速な開発体験」 を提供することです。

従来のフロントエンド開発では、Webpack などのバンドラーがあり、プロジェクト全体をまとめてブラウザ用に変換（ビルド）していました。
これだと、コードを少し書き換えただけでも 全体を再ビルドするため時間がかかる という問題がありました。

Vite はこの問題を解決するために設計されています。

2. Vite の特徴
    (1) 高速な開発サーバー

ES モジュールをそのままブラウザに渡す方式なので、変更箇所だけを即時反映。

その結果、従来のツールより ホットリロードが速い（数百ミリ秒〜数秒で反映）。

(2) 最小限のビルド時間

プロダクション用ビルドは Rollup を内部で使用。

開発中は「必要なファイルだけを変換して配信」するので待ち時間が少ない。

(3) フレームワーク対応が簡単

Vue、React、Svelte、SolidJS などを簡単にサポート。

プラグインも充実しているため、環境構築が簡単。

(4) モダンブラウザ前提

Vite は ES モジュールを使うので、最新ブラウザ向けに最適化されています。

古いブラウザのサポートは追加設定が必要。

3. Vite の仕組み（ざっくり）

開発サーバー

vite dev を実行すると、ブラウザで localhost: 5173 が立ち上がる。

ファイル変更時は差分だけ更新 → 高速。

ビルド

vite build で最適化済みの静的ファイルを生成。

HTML / CSS / JS を圧縮して配布可能。

4. 使い方の基本
# プロジェクト作成
npm create vite @latest my - app

# 開発サーバー起動
cd my - app
npm install
npm run dev   # -> http://localhost:5173

# プロダクションビルド
npm run build


src フォルダ：アプリのソースコードを置く

index.html：ブラウザに最初に読み込まれる HTML

vite.config.js：設定ファイル